import { GameSchemaCheckerResult } from './game-schema-checker-result';

describe('GameSchemaCheckerResult', () => {
  it('should create an instance', () => {
    expect(new GameSchemaCheckerResult()).toBeTruthy();
  });
});
