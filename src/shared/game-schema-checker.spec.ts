import { GameSchemaChecker } from './game-schema-checker';

describe('GameSchemaChecker', () => {
  it('should create an instance', () => {
    expect(new GameSchemaChecker()).toBeTruthy();
  });
});
